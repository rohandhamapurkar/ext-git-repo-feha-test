version: 1.0.0
variables:
  global:
    HOSTING_API_STAGE: canary

events:
  build:
    steps:
      - build:
          script:
            - "nvm use 22"
            - "npm install"
            - "npm run build"
            - 'echo "enable-prebuilt-artifact: true" > acquia_config.yaml'
